[build]
  base    = "frontend"
  command = "npm run build"
  publish = "dist"

# Redirect all routes to index.html so React Router works on reload
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
