services:
  - type: web
    name: crypto-hifo-backend
    env: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false       # You will paste this value in the Render dashboard
      - key: NODE_ENV
        value: production
