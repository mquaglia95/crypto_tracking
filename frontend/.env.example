# Leave blank for local development (Vite proxy handles /api → localhost:3001).
# In production (Netlify), set this to your Render backend URL with no trailing slash.
# Example: VITE_API_URL=https://crypto-hifo-backend.onrender.com
VITE_API_URL=
